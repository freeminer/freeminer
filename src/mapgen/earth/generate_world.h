#include "adapter.h"

bool generate_world(MapgenEarth *mg, const std::vector<ProcessedElement> &elements,
		//const XZBBox& xzbbox,
		//const Ground& ground,
		const Args &args = {});

bool generate_world(MapgenEarth *mg, const ProcessedElement &elements,
		//const XZBBox& xzbbox,
		//const Ground& ground,
		const Args &args = {});
